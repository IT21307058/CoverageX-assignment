// Add custom Cypress commands here (optional)

Cypress.Commands.add('resetBackend', () => {
  // If you add a reset endpoint in backend (see below), call it here:
  // return cy.request('POST', 'http://localhost:8080/__test__/reset');
});
